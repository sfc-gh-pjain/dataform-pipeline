config {
  type: "table",
  schema: "ytstage"
}


select 
  video_id::varchar(255) as video_id
  ,max(to_date(trending_date,'yy.dd.mm')) as last_update
from 
  ${ref("raw_videos_csv")} 
group by video_id